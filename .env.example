# Server Configuration
PORT=3000
NODE_ENV=development

# Authentication Tokens
# Used for admin API access (manage configs, profiles, providers)
ADMIN_TOKEN=change-this-to-your-admin-token

# Used for subscription access (Clash client)
SUBSCRIBE_TOKEN=change-this-to-your-subscribe-token

# Data directory path (relative to project root)
DATA_DIR=./data
